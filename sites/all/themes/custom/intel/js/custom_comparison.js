// Wrap the whole thing in jQuery
(function ($) {
  // Create custom Comparison drupal behavior.
  Drupal.behaviors.custom_comparison = {
    attach: function (context, settings) {
      // @TODO: Create onclick functionality for pages.
      // @TODO: abstract NID from html article entity.
      // @TODO: Store value.
      // @TODO: Create function that will append all value to a url for getting values via AJAX.
    }
  }
})(jQuery);
